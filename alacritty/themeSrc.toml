# Colors (Nevergrove $VARIANT)

[colors.primary]
background = "$BG1"
foreground = "$FG"
dim_foreground = "$FGDIM"

[colors.cursor]
text = "$BG0"
cursor = "$GRAY2"

[colors.selection]
text = "$WHITE"
background = "$VIS"

[colors.normal]
black   = "$BG0"
red     = "$RED"
green   = "$GREEN"
yellow  = "$YELLOW"
blue    = "$BLUE"
magenta = "$PINK"
cyan    = "$TEAL"
white   = "$FG"

[colors.bright]
black   = "$BG4"
red     = "$REDHI"
green   = "$GREENHI"
yellow  = "$YELLOWHI"
blue    = "$BLUEHI"
magenta = "$PINKHI"
cyan    = "$TEALHI"
white   = "$FGHI"

[colors.dim]
black   = "$BLACK"
red     = "$BGRED"
green   = "$BGGREEN"
yellow  = "$BGYELLOW"
blue    = "$BGBLUE"
magenta = "$BGPINK"
cyan    = "$BGTEAL"
white   = "$FGDIM"